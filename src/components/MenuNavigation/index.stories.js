import React from 'react';

// Components
import { MenuNavigation } from '~/components';

export default {
  title: 'Components|MenuNavigation',
  component: MenuNavigation,
};

export const DefaultStory = () => <MenuNavigation />;

DefaultStory.story = {
  name: 'default',
};
